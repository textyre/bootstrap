# Troubleshooting History — 2026-02-05 (Ewwii Migration)

VM: Arch Linux (VirtualBox, NAT 127.0.0.1:2222), user: textyre

## КРИТИЧЕСКИЙ АНАЛИЗ: Миграция Polybar → Ewwii

### TL;DR

**Статус: ПРОВАЛ (ПОВТОРНЫЙ)**. После "исправлений" обнаружена фундаментальная несовместимость: **i3wm игнорирует geometry для ewwii dock-окон**, растягивая их на всю ширину экрана (2560px). Острова отображаются вертикально друг под другом вместо горизонтальной полосы.

---

## Сессия 2: Попытка исправления (2026-02-05, вечер)

### Что было сделано

1. ✅ Исправлен путь `~/.config/eww/` → `~/.config/ewwii/`
2. ✅ Добавлен `export DISPLAY="${DISPLAY:-:0}"` в launch.sh
3. ✅ Добавлены i3 floating rules для ewwii окон
4. ✅ Обновлён i3/config.tmpl: polybar → ewwii
5. ✅ Обновлён theme-switch: polybar → ewwii
6. ✅ Закомментирован polybar в packages.yml
7. ✅ Обновлён molecule/verify.yml
8. ✅ Добавлен polybar в .chezmoiignore
9. ❌ Попытка изменить `windowtype: "dock"` → `"utility"` — не применилось

### Что НЕ работает

| Проблема | Описание | Severity |
|----------|----------|----------|
| **Острова вертикально** | Все 4 окна ewwii стакаются вертикально, каждое 2560x32px | 🔴 CRITICAL |
| **i3 игнорирует geometry** | Запрошенная ширина (100/40/220/440px) заменяется на 2560px | 🔴 CRITICAL |
| **Floating rules не работают** | Окна показывают `floating: "auto_off"` несмотря на `for_window` | 🔴 CRITICAL |
| **polybar всё ещё виден** | Требуется перезапуск i3 для применения изменений | 🟡 MEDIUM |

---

## Root Cause: Несовместимость ewwii dock + i3wm

### Техническое объяснение

**Ewwii** использует `windowtype: "dock"` для создания panel/bar окон. В Wayland/wlroots это работает через layer-shell protocol, позволяя точное позиционирование.

**i3wm на X11** обрабатывает dock-окна по-другому:
- Dock-окна **всегда растягиваются на всю ширину** контейнера
- Geometry hints игнорируются
- Struts резервируют пространство сверху/снизу экрана

**Доказательство из i3 tree:**
```json
{
  "name": "Ewwii - workspaces",
  "rect": {"x": 0, "y": 0, "width": 2560, "height": 32},  // i3 принудительно
  "geometry": {"x": 8, "y": 6, "width": 115, "height": 32}  // ewwii просил
}
```

### Варианты решения

| Вариант | Описание | Сложность | Рекомендация |
|---------|----------|-----------|--------------|
| A. Вернуть Polybar | Polybar нативно поддерживает floating islands | Trivial | ⚠️ Откат |
| B. windowtype: "normal" + floating | Использовать обычные окна + i3 floating rules | Medium | 🟢 Попробовать |
| C. Wayland + Hyprland/Sway | Перейти на Wayland compositor с layer-shell | High | 🔵 Долгосрочно |
| D. Кастомный dock через X11 | Использовать _NET_WM_STRUT_PARTIAL напрямую | High | 🔴 Сложно |

---

## Критический анализ моих ошибок (сессия 2)

### 1. Не проверил визуально СРАЗУ

**Ошибка:** Увидел "ewwii list-windows показывает 4 окна" → объявил успех.

**Реальность:** Окна создаются, но отображаются неправильно (вертикально, full-width).

**Урок:** `list-windows` ≠ "работает". Нужен скриншот или визуальная проверка.

### 2. Не понял архитектуру ewwii + i3

**Ошибка:** Предполагал что `windowtype: "dock"` + `geometry` = floating islands.

**Реальность:** i3wm не поддерживает произвольную геометрию для dock окон. Это фундаментальное ограничение X11/i3, а не баг в конфиге.

**Урок:** Изучить как target WM обрабатывает window types ПЕРЕД выбором технологии.

### 3. Итеративные попытки без понимания

**Ошибка:** Менял `windowtype: "dock"` → `"utility"`, добавлял floating rules — ничего не помогло.

**Реальность:** Изменения применялись, но проблема глубже — ewwii создаёт окна определённым образом, i3 их обрабатывает по-своему.

**Урок:** Остановиться и прочитать документацию/issues ПЕРЕД очередной попыткой.

### 4. Не проверил chezmoi sync

**Ошибка:** Думал что `chezmoi apply --force` всегда обновляет файлы.

**Реальность:** На VM могут быть старые версии файлов если:
- Git репозиторий на VM не обновлён
- Файлы модифицированы локально на VM
- Chezmoi cache не инвалидирован

**Урок:** После `chezmoi apply` ПРОВЕРИТЬ содержимое файла на target.

### 5. Игнорирование DISPLAY в SSH

**Ошибка:** Запускал ewwii через SSH без DISPLAY.

**Реальность:** GTK требует `DISPLAY=:0` для инициализации.

**Исправлено:** Добавлен `export DISPLAY="${DISPLAY:-:0}"` в launch.sh.

---

## Текущее состояние файлов

### Исправлено (локально)

| Файл | Изменение | Статус на VM |
|------|-----------|--------------|
| ewwii.rhai.tmpl | listen(), динамические WS, путь ewwii | ✅ Применено |
| ewwii.scss.tmpl | Без изменений | ✅ OK |
| executable_launch.sh | DISPLAY export | ✅ Применено |
| i3/config.tmpl | polybar→ewwii, floating rules | ⚠️ Требует i3 reload |
| theme-switch | polybar→ewwii | ✅ Применено |
| packages.yml | polybar закомментирован | ✅ Применено |
| molecule/verify.yml | polybar→ewwii | ✅ Применено |
| .chezmoiignore | polybar добавлен | ✅ Применено |

### Не исправлено (блокеры)

| Проблема | Файл | Причина |
|----------|------|---------|
| **Острова вертикально** | ewwii.rhai.tmpl | i3 игнорирует geometry для dock |
| **Full-width окна** | ewwii.rhai.tmpl | i3 растягивает dock на 100% |
| **workspaces.sh не запускается** | scripts/workspaces.sh | Нет ошибки, но и нет вывода |

---

## Сравнение: Polybar vs Ewwii на i3/X11

| Аспект | Polybar | Ewwii |
|--------|---------|-------|
| Floating islands | ✅ Нативная поддержка | ❌ Требует workaround |
| Динамические WS | ✅ IPC module | ⚠️ listen() + скрипты |
| Позиционирование | ✅ offset-x, width | ❌ Игнорируется i3 |
| i3 совместимость | ✅ Полная | ❌ Только Wayland/wlroots |
| Конфигурация | INI + shell | Rhai (сложнее) |

---

## Решено из предыдущей сессии

- [x] **Путь ~/.config/eww → ewwii** — исправлено в ewwii.rhai.tmpl
- [x] **listen() для workspaces** — добавлен `listen("workspaces_json", ...)`
- [x] **listen() для ws_bar_width** — добавлен `listen("ws_bar_width", ...)`
- [x] **listen() для ws_add_x** — добавлен `listen("ws_add_x", ...)`
- [x] **Динамическая функция workspaces()** — принимает JSON, парсит, рендерит
- [x] **Скрипты calc-ws-width.sh, calc-add-offset.sh** — созданы и работают
- [x] **Константы унифицированы** — SEP_GAP везде
- [x] **DISPLAY для SSH** — export в launch.sh

---

## Рекомендации

### Немедленно (если продолжать с ewwii)

1. **Попробовать `windowtype: "normal"`:**
   ```rhai
   defwindow("workspaces", #{
       windowtype: "normal",  // вместо "dock"
       // ...
   })
   ```
   + добавить в i3 config:
   ```
   for_window [title="^Ewwii - "] floating enable, sticky enable, border none
   ```

2. **Проверить на Wayland:** Если есть возможность — запустить с Sway/Hyprland.

### Альтернатива: Откат на Polybar

Polybar уже настроен и работает. Для floating islands:
```ini
[bar/workspaces]
width = 100
offset-x = 8
```

Это **гарантированно работает** на i3/X11.

---

## Итог сессии 2

**Статус миграции: ПРОВАЛ**

- Фундаментальная несовместимость ewwii dock windows + i3wm
- Код исправлен, но i3 игнорирует geometry
- Острова отображаются вертикально (2560x32 каждое)
- Polybar всё ещё работает поверх

**Оценка качества работы сессии 2: 4/10**
- +2 за обнаружение root cause (i3 + dock = несовместимость)
- +2 за исправление мелких багов (путь, DISPLAY, константы)
- -3 за трату времени на итеративные попытки без понимания
- -3 за отсутствие визуальной проверки на ранних этапах

**Рекомендуемое действие:** Откат на Polybar ИЛИ миграция на Wayland compositor.

---

## Извлечённые уроки

1. **Визуальная проверка > CLI проверка** — "окна открылись" ≠ "работает как надо"
2. **Изучить target platform** — i3/X11 имеет ограничения для dock windows
3. **Читать issues** — проблема ewwii+i3 наверняка известна
4. **Не менять технологии без причины** — Polybar работал, ewwii — эксперимент
5. **Тестировать инкрементально** — один виджет → проверка → следующий
