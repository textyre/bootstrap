# Детальный анализ конфигурации Polybar

**Дата:** 2026-02-05
**Статус:** Полная инвентаризация текущего состояния конфигурации
**Цель:** Документирование всей функциональности для миграции/рефакторинга

---

## 1. ОБЗОР АРХИТЕКТУРЫ

### 1.1 Дизайн: "Три плавающих острова"

Polybar реализован в виде трёх независимых floating bars, создающих визуальный эффект плавающих нпанелей вверху экрана:

```
[Workspaces]          [Clock]                    [System Info]
  (левый)          (по центру)                   (правый)
```

**Особенности архитектуры:**
- Каждый бар — отдельный экземпляр polybar
- `override-redirect = true` — окна не управляются WM (i3)
- Полностью прозрачный фон (`background = #00000000`)
- Каждый бар запускается для каждого монитора отдельно
- `enable-ipc = true` — поддержка горячей перезагрузки

**Автозапуск:** Прописан в i3 config (line 187):
```bash
exec_always --no-startup-id ~/.config/polybar/launch.sh
```

---

## 2. ОПРЕДЕЛЁННЫЕ БАРЫ И ИХ СТРУКТУРА

### 2.1 Bar: workspaces (ЛЕВЫЙ ОСТРОВ)

**Путь конфига:** `dotfiles/dot_config/polybar/config.ini.tmpl` (lines 24-51)

| Параметр | Значение | Примечание |
|----------|----------|-----------|
| `width` | `${env:WS_BAR_WIDTH:100}` | Динамический расчёт (см. launch.sh) |
| `offset-x` | `8` (gaps_outer) | От левого края |
| `offset-y` | `6` (bar_offset_y) | От верхнего края |
| `height` | `32` | bar_height |
| `radius` | `14` | Скруглённые углы |
| `border-size` | `1` | Тонкая рамка |
| `background` | `#dd0a0a0a` | Полупрозрачный чёрный (DD = альфа 85%) |
| `foreground` | Цвет из темы (fg) | |
| `modules-center` | `workspaces` | Единственный модуль |

**Шрифты:**
- Font-0: Mono bold (10px) — основной
- Font-1: Nerd Font Icon (16px) — для иконок
- Font-2: Mono (10px) — вспомогательный

**Взаимодействие:**
- Левый клик по workspace → переключение (`i3-msg workspace number $N`)
- Правый клик на workspace 4+ → контекстное меню через rofi
- IPC-enabled → реагирует на i3 события

---

### 2.2 Bar: workspace-add (ЛЕВЫЙ ОСТРОВ - КНОПКА ДОБАВЛЕНИЯ)

**Путь конфига:** `dotfiles/dot_config/polybar/config.ini.tmpl` (lines 54-82)

| Параметр | Значение | Примечание |
|----------|----------|-----------|
| `width` | `40` | Узкая кнопка |
| `offset-x` | `WS_BAR_WIDTH + 4` | Динамический расчёт относительно workspace бара |
| `offset-y` | `6` | Совпадает с workspace баром |
| `height` | `32` | Совпадает с workspace баром |
| `modules-center` | `workspace-add-btn` | Кнопка "+" |

**Модуль:**
```ini
[module/workspace-add-btn]
type = custom/text
format = "%{T2}+%{T-}"
format-foreground = ${colors.accent}
click-left = ~/.config/polybar/scripts/add-workspace.sh
```

Кнопка "+" (U+002B) в цвете accent. При клике вызывает скрипт добавления воркспейса.

---

### 2.3 Bar: clock (ЦЕНТРАЛЬНЫЙ ОСТРОВ)

**Путь конфига:** `dotfiles/dot_config/polybar/config.ini.tmpl` (lines 85-112)

| Параметр | Значение | Примечание |
|----------|----------|-----------|
| `width` | `220` | bar_width_clock |
| `offset-x` | `50%:-110` | Центрирование: 50% экрана минус половина ширины |
| `offset-y` | `6` | bar_offset_y |
| `height` | `32` | bar_height |
| `modules-center` | `date` | Internal date модуль |

**Модуль date:**
```ini
[module/date]
type = internal/date
interval = 1
date = "%a %b %d"
time = "%H:%M"
format = <label>
label = "%{T2}%{T-}  %date%    %time%"
click-left = gsimplecal &
```

Отображает:
- Календарь (иконка U+F073)
- День недели + дата (Wed Feb 05)
- Время (14:23)

Клик — открывает `gsimplecal` (простой календарь).

---

### 2.4 Bar: system (ПРАВЫЙ ОСТРОВ)

**Путь конфига:** `dotfiles/dot_config/polybar/config.ini.tmpl` (lines 115-144)

| Параметр | Значение | Примечание |
|----------|----------|-----------|
| `width` | `440` | bar_width_system |
| `offset-x` | `100%:-448` | От правого края (100% - width - gaps_outer) |
| `offset-y` | `6` | bar_offset_y |
| `height` | `32` | bar_height |
| `modules-left` | `network sep volume sep cpu sep memory` | Левая часть |
| `modules-right` | `sep controlcenter sep powermenu sep tray` | Правая часть |

Содержит системную информацию и управление.

---

## 3. ВСЕ МОДУЛИ (ПОЛНЫЙ СПИСОК)

### 3.1 Встроенные внутренние модули (internal/*)

#### module/date
- **Тип:** `internal/date`
- **Интервал обновления:** 1 сек
- **Отображает:** День, дата, время
- **Иконка:** U+F073 (календарь)

#### module/network
- **Тип:** `internal/network`
- **Интервал:** 3 сек
- **Отображает:** IP-адрес или "disconnected"
- **Иконка:** U+F1EB (WiFi)
- **Клик:** Открывает `nmtui` в alacritty
- **Цвета:**
  - Connected: success (зелёный)
  - Disconnected: urgent (красный)

#### module/volume
- **Тип:** `internal/pulseaudio`
- **Отображает:** Процент громкости или "muted"
- **Иконки:**
  - Громкость: U+F028 (динамик)
  - Mute: U+F6A9 (перечёркнутый динамик)
- **Клик:** `pavucontrol`
- **Скролл:** +5/-5% громкости через `pamixer`

#### module/cpu
- **Тип:** `internal/cpu`
- **Интервал:** 2 сек
- **Отображает:** % CPU использования
- **Иконка:** U+F2DB (процессор)
- **Цвет:** info (голубой)

#### module/memory
- **Тип:** `internal/memory`
- **Интервал:** 3 сек
- **Отображает:** % RAM использования
- **Иконка:** U+F538 (чип памяти)
- **Цвет:** warning (жёлтый)

#### module/tray
- **Тип:** `internal/tray`
- **Размер иконок:** 18px
- **Spacing:** 4px
- **Примечание:** Пуст (nm-applet удалён)

### 3.2 Пользовательские скриптовые модули (custom/script)

#### module/workspaces
- **Тип:** `custom/script`
- **Скрипт:** `~/.config/polybar/scripts/workspaces.sh`
- **Режим:** `tail = true` — постоянно слушает i3 события
- **Функционал:** см. раздел 4.1

### 3.3 Пользовательские текстовые модули (custom/text)

#### module/workspace-add-btn
- **Текст:** "+" (U+002B)
- **Цвет:** accent
- **Клик:** `~/.config/polybar/scripts/add-workspace.sh`

#### module/sep
- **Текст:** Пустой offset `%{O22}` (22px gap)
- **Назначение:** Разделитель между модулями

#### module/controlcenter
- **Текст:** ⚙ (U+F013)
- **Цвет:** accent
- **Клик:** `~/.config/rofi/scripts/controlcenter.sh`

#### module/powermenu
- **Текст:** ⏻ (U+F011)
- **Цвет:** urgent (красный)
- **Клик:** `~/.config/rofi/scripts/powermenu.sh`

---

## 4. СКРИПТЫ В SCRIPTS/ (ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ)

### 4.1 workspaces.sh.tmpl (ГЛАВНЫЙ СКРИПТ ВОРКСПЕЙСОВ)

**Путь:** `dotfiles/dot_config/polybar/scripts/executable_workspaces.sh.tmpl`
**Язык:** bash
**Размер:** ~133 строки

**Задача:** Отображение всех воркспейсов с динамическими иконками и состоянием

**Алгоритм:**

```
1. Загрузить константы:
   - EDGE_PADDING=12 (захардкожен)
   - GAP=22 (из layout.toml)
   - Цвета темы (FOCUSED, OCCUPIED, EMPTY, URGENT)

2. Инициализировать WS_NAMES (базовые иконки):
   - 1: U+EE98 86 (Веб)
   - 2: U+EF84 A1 (Код)
   - 3: U+EF84 A0 (Терминал)

3. Загрузить custom-иконки из ~/.config/polybar/workspace-icons.conf
   Формат: `4:📞` (num:icon)

4. Для каждого воркспейса (1-10):
   - Запросить состояние: i3-msg -t get_workspaces
   - Определить статус: focused/occupied/empty/urgent
   - Применить цвет и подчёркивание
   - Добавить в вывод с gaps

5. Для воркспейсов 4+ (динамические):
   - Если воркспейс существует, добавить right-click handler
   - Right-click → workspace-menu.sh

6. Выводить результат в polybar

7. Подписаться на i3 workspace события:
   i3-msg -t subscribe -m '["workspace"]' | while read...
   (перерендеривать при каждом событии)
```

**Особенности:**
- Custom icons перезагружаются при каждом рендере → изменения видны без restart
- Воркспейсы 1-3 всегда отображаются (даже если пусты)
- Воркспейсы 4-10 отображаются только если существуют
- Использует polybar markup: `%{T2}` (шрифт), `%{F}` (цвет), `%{A}` (клики), `%{O}` (offset)

---

### 4.2 add-workspace.sh (СОЗДАНИЕ НОВОГО ВОРКСПЕЙСА)

**Путь:** `dotfiles/dot_config/polybar/scripts/executable_add-workspace.sh`
**Язык:** bash
**Размер:** ~50 строк

**Задача:** Добавить новый воркспейс с выбранной иконкой

**Алгоритм:**

```
1. Показать rofi меню выбора иконки:
   ● Круг (по умолчанию)
   󰀛 Terminal
   󰀫 Code
   ... (10 вариантов)

2. Пользователь выбирает иконку

3. Найти первый свободный воркспейс (4-10):
   for i in {4..10}:
     if i3-msg -t get_workspaces | jq не содержит $i:
       break

4. Сохранить иконку в ~/.config/polybar/workspace-icons.conf:
   echo "4:󰀛" >> $ICONS_FILE

5. Переключиться на новый воркспейс:
   i3-msg "workspace number $i"

6. Перезапустить polybar (КОСТЫЛЬ):
   sleep 0.1
   ~/.config/polybar/launch.sh &
   (нужна динамическая пересчёт ширины)
```

**Проблемы (из документации 2026-02-05):**
- Вызывает `launch.sh` → перезапускает ВСЕ 4 бара вместо одного
- Видимый флаш экрана
- Правильное решение: передать MONITOR явно и обновить только workspace+workspace-add бары

---

### 4.3 close-workspace.sh (ЗАКРЫТИЕ ВОРКСПЕЙСА)

**Путь:** `dotfiles/dot_config/polybar/scripts/executable_close-workspace.sh`
**Язык:** bash
**Размер:** ~22 строки

**Задача:** Закрыть динамический воркспейс

**Алгоритм:**

```
1. Получить номер воркспейса из параметра: WS_NUM=$1

2. Переместить все окна в workspace 1:
   i3-msg "[workspace=$WS_NUM] move to workspace 1"

3. Переключиться на workspace 1:
   i3-msg "workspace 1"

4. Удалить иконку из конфига:
   sed -i "/^${WS_NUM}:/d" ~/.config/polybar/workspace-icons.conf

5. Перезапустить polybar:
   sleep 0.2
   ~/.config/polybar/launch.sh &
```

**Особенности:**
- При закрытии воркспейса его окна сохраняются (переходят в ws1)
- i3 сам удалит пустой воркспейс из памяти

---

### 4.4 change-workspace-icon.sh (СМЕНА ИКОНКИ)

**Путь:** `dotfiles/dot_config/polybar/scripts/executable_change-workspace-icon.sh`
**Язык:** bash
**Размер:** ~39 строк

**Задача:** Изменить иконку существующего воркспейса

**Алгоритм:**

```
1. Получить номер воркспейса: WS_NUM=$1

2. Показать rofi меню выбора иконки (10 вариантов)

3. Если пользователь выбрал:
   - Распарсить иконку из первого поля (awk '{print $1}')

4. Удалить старую иконку для этого воркспейса:
   sed -i "/^${WS_NUM}:/d" ~/.config/polybar/workspace-icons.conf

5. Добавить новую:
   echo "${WS_NUM}:${ICON}" >> $ICONS_FILE

6. Polybar перезагружает иконки автоматически (tail = true)
   (НЕ требует перезапуска)
```

**Отличие от add-workspace.sh:**
- НЕ перезапускает polybar (иконки перезагружаются динамически)
- Работает только для существующих воркспейсов

---

### 4.5 workspace-menu.sh (КОНТЕКСТНОЕ МЕНЮ)

**Путь:** `dotfiles/dot_config/polybar/scripts/executable_workspace-menu.sh`
**Язык:** bash
**Размер:** ~21 строка

**Задача:** Контекстное меню для воркспейсов (right-click на ws 4+)

**Алгоритм:**

```
1. Получить номер воркспейса: WS_NUM=$1

2. Показать meню через rofi:
   "Сменить иконку"
   "Закрыть воркспейс"

3. В зависимости от выбора:
   - "Сменить иконку" → change-workspace-icon.sh $WS_NUM
   - "Закрыть воркспейс" → close-workspace.sh $WS_NUM
```

**Интеграция:** Вызывается из workspaces.sh при right-click на ws 4+

---

### 4.6 launch.sh (ГЛАВНЫЙ ЗАПУСКАТЕЛЬ)

**Путь:** `dotfiles/dot_config/polybar/executable_launch.sh`
**Язык:** bash
**Размер:** ~39 строк

**Задача:** Запустить все 4 бара на все мониторы с правильной шириной

**Алгоритм:**

```
1. Убить все существующие polybar процессы:
   killall -q polybar
   while pgrep polybar > /dev/null; do sleep 0.2; done

2. Рассчитать ширину workspace бара:
   GAPS_OUTER=8 (из layout.toml)
   EDGE_PADDING=12 (ЗАХАРДКОЖЕН)
   GAP=22 (ЗАХАРДКОЖЕН, должен быть из layout.toml)
   ICON_WIDTH=16 (связан с font.icon_size)
   MIN_WS=3 (минимум воркспейсов для отображения)

   WS_COUNT = max(actual_ws_count, MIN_WS)
   WS_BAR_WIDTH = 2*EDGE_PADDING + WS_COUNT*ICON_WIDTH + (WS_COUNT-1)*GAP
   WS_ADD_OFFSET = GAPS_OUTER + WS_BAR_WIDTH + 4

3. Экспортировать env vars:
   export WS_BAR_WIDTH
   export WS_ADD_OFFSET

4. Для каждого подключённого монитора (xrandr):
   MONITOR=$m polybar --reload workspaces &
   MONITOR=$m polybar --reload workspace-add &
   MONITOR=$m polybar --reload clock &
   MONITOR=$m polybar --reload system &

5. Если xrandr отсутствует:
   Запустить все 4 бара без MONITOR (fallback)
```

**Важные константы:**

| Константа | Значение | Статус | Проблема |
|-----------|----------|--------|----------|
| GAPS_OUTER | 8 | В launch.sh | ✓ Совпадает с layout.toml |
| EDGE_PADDING | 12 | Захардкожен в 3 местах | ❌ Дублирование |
| GAP | 22 | Захардкожен | ❌ Должен быть из layout.toml |
| ICON_WIDTH | 16 | Захардкожен | ❌ Связан с font.icon_size но неявно |
| MIN_WS | 3 | Захардкожен | ✓ Логично |

---

## 5. ДИНАМИЧЕСКАЯ ЛОГИКА ВОРКСПЕЙСОВ

### 5.1 Структура хранения иконок

**Файл:** `~/.config/polybar/workspace-icons.conf` (не в git)

**Формат:**
```
4:☆
5:󰀨
6:󰀪
...
10:󰌐
```

Создаётся и модифицируется скриптами:
- `add-workspace.sh` — добавляет строку
- `change-workspace-icon.sh` — удаляет старую, добавляет новую
- `close-workspace.sh` — удаляет строку при закрытии

### 5.2 Жизненный цикл воркспейса

```
┌─────────────┐
│  Щёлкнуть   │
│ "+" кнопку  │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  add-workspace.sh           │
│  1. Показать meню иконок    │
│  2. Найти свободный WS (4+) │
│  3. Сохранить иконку        │
│  4. Переключиться на WS     │
│  5. launch.sh (перезапуск)  │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Воркспейс создан и видим   │
│  в workspace bar            │
└──────┬──────────────────────┘
       │
       ▼
   [3 варианта]

   1. Right-click на иконку
      │
      ▼
   ┌──────────────────────────────┐
   │ workspace-menu.sh            │
   │ ▢ Сменить иконку             │
   │ ▢ Закрыть воркспейс          │
   └──────┬───────────────────────┘
          │
   "Сменить" → change-workspace-icon.sh (динамически)
          │
   "Закрыть" → close-workspace.sh
          │
          ▼
      ┌────────────────────────────┐
      │ Окна в WS перемещены в WS1 │
      │ WS удалён из конфига       │
      │ polybar перезапущен        │
      └────────────────────────────┘
```

### 5.3 Состояния воркспейса (visual)

**في workspaces.sh (lines 74-89):**

```bash
if [[ "$urgent" == "true" ]]; then
    color="$URGENT"          # Красный — срочно (есть notification)
elif [[ "$focused" == "true" ]]; then
    color="$FOCUSED"         # Accent (фиолетовый в dracula)
    underline="%{u...}%{+u}" # Подчёркивание
else
    color="$OCCUPIED"        # Белый — есть окна но не active
fi

# Если воркспейс пуст (не существует):
color="$EMPTY"              # Серый — никогда не создавался
```

**Визуальное отображение:**
- Focused: Accent + подчёркивание
- Occupied: Foreground (белый)
- Empty: Foreground-dim (серый)
- Urgent: Urgent (красный)

---

## 6. ВНЕШНИЕ ЗАВИСИМОСТИ

### 6.1 Шрифты

**Монопространственный шрифт:**
- `JetBrainsMono Nerd Font Mono` — размер 11px (моно), 10px (bar)
- Все текстовые элементы

**Nerd Font иконки:**
- `JetBrainsMono Nerd Font Mono` — размер 16px
- Private Use Area (U+E000–U+F8FF)
- Пример: U+F028 (volume) = 0xEF 0xB8 0xA8

**Требование:** Шрифт ДОЛЖЕН быть установлен и содержать Nerd Font glyphs
```bash
fc-list | grep "JetBrainsMono Nerd"
```

### 6.2 Иконки (Polybar markup)

**Используемые иконы в config.ini:**

| Иконка | Unicode | Назначение |
|--------|---------|-----------|
| 🗓️ | U+F073 | Календарь (date модуль) |
| 📶 | U+F1EB | WiFi/Network |
| ⚠️ | U+F071 | Warning (не используется, deprecated) |
| 🔊 | U+F028 | Volume |
| 🔇 | U+F6A9 | Mute |
| ⚙️ | U+F2DB | CPU |
| 💾 | U+F538 | Memory |
| ⚙️ | U+F013 | Settings (Control Center) |
| ⏻️ | U+F011 | Power (Power Menu) |

**Используемые в workspace icons (от пользователя):**

```
● Круг (U+25CF)
󰀛 Terminal
󰀫 Code
󰀨 Browser
󰄊 Files
󰊁 Chat
󰓎 Music
󱨎 Video
󰮗 Gaming
󰌱 Settings
󰒕 Notes
```

### 6.3 Внешние команды (dependencies)

**Обязательные:**
- `i3-msg` — IPC с i3 WM (для workspace переключения/создания)
- `jq` — JSON парсинг (parse i3-msg output)
- `xrandr` — обнаружение мониторов (graceful fallback если отсутствует)

**Для модулей:**
- `pamixer` — управление громкостью (volume модуль)
- `rofi` — меню выбора иконок (add-workspace, change-icon, context-menu)

**Опциональные (UI улучшение):**
- `gsimplecal` — календарь при клике на date (если отсутствует, клик безопасно игнорируется)
- `pavucontrol` — GUI для громкости (если отсутствует, клик безопасно игнорируется)
- `notify-send` — системные уведомления (add-workspace при max workspaces)

**Из i3 config (related):**
- `alacritty` — терминал (для nmtui)
- `flameshot` — скриншоты (из i3 config)
- `playerctl` — медиа контроль (из i3 config)
- `brightnessctl` — подсветка экрана (из i3 config)

### 6.4 Темы Rofi

**Используемые файлы:**

| Файл | Назначение | Путь |
|------|-----------|------|
| `icon-select.rasi.tmpl` | Меню выбора иконок (grid 2x5) | `~/.config/rofi/themes/` |
| `context-menu.rasi.tmpl` | Right-click меню (dropdown) | `~/.config/rofi/themes/` |

Обе темы:
- Используют цвета из `.chezmoidata/themes.toml`
- Применяют border-radius из `.layout.corner_radius` (10px)
- Отрендеиваются через chezmoi

---

## 7. ПАРАМЕТРЫ КОНФИГУРАЦИИ (LAYOUT.TOML)

**Файл:** `dotfiles/.chezmoidata/layout.toml`

| Параметр | Значение | Где используется | Статус |
|----------|----------|------------------|--------|
| `gaps_inner` | 4 | i3 config | ✓ Sync |
| `gaps_outer` | 8 | polybar offset-x, launch.sh | ✓ Sync |
| `gaps_top` | 48 | i3 config (top gap для polybar) | ✓ Sync |
| `border_size` | 1 | i3 config | ✓ Sync |
| `corner_radius` | 10 | rofi темы | ✓ Sync |
| `bar_height` | 32 | Все 4 бара | ✓ Sync |
| `bar_offset_y` | 6 | Все 4 бара | ✓ Sync |
| `bar_border` | 1 | Все 4 бара | ✓ Sync |
| `bar_radius` | 14 | Все 4 бара | ✓ Sync |
| `network_interface_type` | "wired" | polybar network модуль | ✓ Sync |
| `bar_width_workspaces` | 100 | workspace bar (baseline) | ⚠️ Переопределяется launch.sh |
| `bar_width_clock` | 220 | clock bar | ✓ Sync |
| `bar_width_system` | 440 | system bar | ✓ Sync |
| `bar_padding` | 4 | clock, system бары | ✓ Sync |
| `bar_padding_ws` | 0 | workspace bar | ✓ Sync |
| `sep_gap` | 22 | separator offset, launch.sh | ⚠️ Дублирование в launch.sh |
| `font_offset` | 3 | font-0/1/2 (Y offset при рендере) | ✓ Sync |

**Проблемы (из 2026-02-05 диагностики):**
1. ❌ EDGE_PADDING=12 захардкожен в launch.sh и workspaces.sh.tmpl — должен быть в layout.toml
2. ❌ GAP=22 захардкожен в launch.sh (дублирован с sep_gap в layout.toml)
3. ❌ ICON_WIDTH=16 захардкожен в launch.sh (должен быть связан с font.icon_size)

---

## 8. ЦВЕТОВЫЕ СХЕМЫ И ТЕМЫ

**Файл:** `dotfiles/.chezmoidata/themes.toml`

### 8.1 Dracula (Catppuccin Mocha)

```toml
[themes.dracula]
bg              = "#11111b"      # Очень тёмный фиолет
bg_alt          = "#0b0b14"      # Ещё темнее
bg_light        = "#1e1e2e"      # Светлее для выделения
fg              = "#cdd6f4"      # Белый текст
fg_dim          = "#585b70"      # Серый вспомогательный
accent          = "#cba6f7"      # Фиолетовый (Mauve)
accent2         = "#f5c2e7"      # Розовый
urgent          = "#f38ba8"      # Красный-розовый
success         = "#a6e3a1"      # Зелёный
warning         = "#f9e2af"      # Жёлтый
info            = "#89b4fa"      # Голубой
border_active   = "#cba6f7"      # Фиолетовый (активное окно)
border_inactive = "#313244"      # Тёмный
border_urgent   = "#f38ba8"      # Красный (срочное)
```

**Использование в polybar:**
```ini
[colors]
background     = #00000000
foreground     = #cdd6f4
foreground-dim = #585b70
accent         = #cba6f7
accent2        = #f5c2e7
urgent         = #f38ba8
success        = #a6e3a1
warning        = #f9e2af
info           = #89b4fa
island         = #dd11111b          ; Полупрозрачный бар
island-border  = #66cba6f7          ; Граница бара
```

### 8.2 Monochrome

```toml
[themes.monochrome]
bg              = "#0a0a0a"      # Чёрный
bg_alt          = "#050505"      # Очень чёрный
bg_light        = "#161616"      # Серый
fg              = "#c0c0c0"      # Светло-серый текст
fg_dim          = "#4a4a4a"      # Тёмно-серый
accent          = "#d4d4d4"      # Очень светло-серый
accent2         = "#808080"      # Средне-серый
urgent          = "#b04040"      # Тёмно-красный
success         = "#808080"      # Серый
warning         = "#a0a0a0"      # Светло-серый
info            = "#606060"      # Средне-серый
border_active   = "#383838"      # Тёмно-серый (активное)
border_inactive = "#161616"      # Очень тёмный
border_urgent   = "#b04040"      # Тёмно-красный
```

### 8.3 Цветовой формат в Polybar (#AARRGGBB)

**ВАЖНО:** Polybar использует формат `#AARRGGBB` (альфа ПЕРВЫЙ), а не `#RRGGBBAA`:

**Неправильно:**
```ini
background = {{ $t.bg }}dd  # → #0a0a0add (НЕПРАВИЛЬНО: alpha=0x0a, RGB=0a0add → синий!)
```

**Правильно:**
```ini
background = #dd{{ trimPrefix "#" $t.bg }}  # → #dd0a0a0a (OK: alpha=0xdd, RGB=0a0a0a)
island-border = #66{{ trimPrefix "#" $t.border_active }}
```

---

## 9. ОСОБЕННОСТИ КОНФИГУРАЦИИ

### 9.1 IPC-поддержка (горячая перезагрузка)

```ini
enable-ipc = true
cursor-click = pointer
```

Позволяет:
- `polybar-msg cmd restart` — перезагрузить конфиг без killall
- Изменения в `~/.config/polybar/workspace-icons.conf` видны немедленно (tail = true в workspaces модуле)

**Ограничение:** `polybar-msg cmd restart` НЕ перечитывает env vars (WS_BAR_WIDTH остаётся старым) — поэтому используется полный `launch.sh` при добавлении/удалении воркспейса

### 9.2 Переводы (локализация)

Все текстовые элементы на **русском** (в скриптах):
- Меню workspace-menu.sh: "Сменить иконку", "Закрыть воркспейс"
- Rofi label: "Иконка воркспейса"
- Notify: "Максимум 10 воркспейсов"

**Проблема:** Нет i18n — при смене языка системы не обновляются автоматически

### 9.3 Многомониторная поддержка

**Реализация:** launch.sh перебирает мониторы через xrandr:

```bash
for m in $(xrandr --query | grep " connected" | cut -d" " -f1); do
    MONITOR=$m polybar --reload workspaces &
    # ... остальные бары
done
```

**Результат:** На каждом мониторе — независимые 4 бара (16 процессов polybar на 4 мониторах)

**Ограничения:**
- workspace-icons.conf глобальный (одинаковые иконки на всех мониторах)
- Clock и System батят одинаково на всех мониторах

### 9.4 Graceful fallback

```bash
if type "xrandr" >/dev/null 2>&1; then
    # Multi-monitor mode
else
    # Fallback: single bar per type
fi
```

Если xrandr отсутствует, запускаются 4 бара без MONITOR переменной (не будут работать корректно на multi-monitor)

---

## 10. ТЕХНИЧЕСКИЙ ДОЛГ И ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### 10.1 Критичные

| Проблема | Статус | Влияние |
|----------|--------|---------|
| **Динамическая ширина не верифицирована** | 🔴 Не тестировано | При добавлении/удалении ws бар может переполниться |
| **EDGE_PADDING, GAP захардкожены в 3 местах** | 🔴 Дублирование | Изменение одного значения требует правки 3 файлов |
| **launch.sh перезапускает ВСЕ бары** | 🟠 Костыль | Видимый флаш экрана при добавлении ws |

### 10.2 Требуют architectural fix

| Проблема | Решение | Сложность |
|----------|---------|-----------|
| EDGE_PADDING из 3 мест → layout.toml | Конвертировать launch.sh в .tmpl или читать из config.ini | Medium |
| GAP дублирован (sep_gap ↔ launch.sh) | Использовать одно значение везде | Low |
| ICON_WIDTH неявно связан с font.icon_size | Явно связать или параметризировать | Low |
| Polybar перезапуск вместо IPC-reload | Получить MONITOR из контекста клика, вызвать polybar-msg | High |
| Locale: POSIX → UTF-8 requirement | Документировать как обязательную систему-требование | Low (документация) |

### 10.3 Низкий приоритет

- Dracula тема не тестирована визуально (только monochrome)
- Rofi themes не проверены на контраст
- Tray модуль пуст (nm-applet удалён)
- Владение bashrc: Ansible vs chezmoi (архитектурная двусмысленность)

---

## 11. ИНТЕГРАЦИЯ С ДРУГИМИ КОМПОНЕНТАМИ

### 11.1 i3 WM

**Интеграция в i3 config (lines 187):**
```bash
exec_always --no-startup-id ~/.config/polybar/launch.sh
```

**Взаимодействие:**
- Polybar подписан на i3 workspace события
- Клики в polybar отправляют `i3-msg` команды
- i3 границы и gaps синхронизированы с polybar параметрами

**Зависимость:** polybar полностью зависит от i3-msg для получения/изменения workspace данных

### 11.2 Rofi (меню)

**Используемые скрипты:**
- `~/.config/rofi/scripts/controlcenter.sh` — клик на ⚙️
- `~/.config/rofi/scripts/powermenu.sh` — клик на ⏻
- Внутренние: rofi меню иконок (add-workspace, change-icon)

**Темы:**
- `icon-select.rasi.tmpl` — grid 2 колонны, 5 строк
- `context-menu.rasi.tmpl` — dropdown, 2 пункта

### 11.3 Chezmoi (конфиги)

**Управляемые файлы:**
- `config.ini.tmpl` — цвета из themes.toml, размеры из layout.toml
- `workspaces.sh.tmpl` — цвета, gap values
- Все rofi .rasi.tmpl — цвета

**Темплейтирование:**
```go
{{ $t := index .themes .theme_name }}  // Получить текущую тему
{{ .layout.bar_height }}               // Layout параметры
{{ .font.icon_size }}                  // Шрифты
```

### 11.4 Ansible (deployment)

**Установка polybar:**
```yaml
# Вероятно в packages_wm
- polybar
- jq
- rofi
```

**Скрипты зависимости:**
- `pamixer` (volume control)
- `rofi` (меню)
- `gsimplecal` (опционально)
- `alacritty` (для nmtui)

---

## 12. МИГРАЦИЯ / ПЕРЕНОС — ЧЕКЛІСТ

### Что нужно перенести

- [x] Главный конфиг `config.ini.tmpl` (274 строк, chezmoi template)
- [x] Launch скрипт `launch.sh` (39 строк, constants)
- [x] Workspace скрипт `workspaces.sh.tmpl` (133 строки, chezmoi template)
- [x] Add-workspace скрипт (50 строк)
- [x] Close-workspace скрипт (22 строки)
- [x] Change-icon скрипт (39 строк)
- [x] Context-menu скрипт (21 строка)
- [x] Rofi themes (2 файла .rasi.tmpl)
- [x] Layout параметры (layout.toml)
- [x] Цветовые схемы (themes.toml)

### Что нужно скопировать 1:1

- Все Nerd Font иконки (Unicode codepoints)
- Все цветовые значения (#AARRGGBB формат)
- Роли в polybar markup (%{T}, %{F}, %{O}, %{A})
- Shell командировки (set -euo pipefail, etc)

### Что может быть переделано

- Layout параметры: вытащить захардкожены значения (EDGE_PADDING, GAP, ICON_WIDTH)
- Polybar restart logic: вместо brute-force перезапуска всех баров
- Rofi integration: может быть переделано на другой UI фреймворк
- i3-msg dependency: будет другой для других WM (Hyprland, Sway, etc)

---

## 13. РЕЗЮМЕ

**Текущее состояние:** Полностью функциональная 3-bar floating island конфигурация polybar с динамическими рабочими пространствами, иконками, и горячей перезагрузкой.

**Количество компонентов:**
- 1 главный конфиг (config.ini.tmpl)
- 1 launcher скрипт
- 4 модуля (встроены в config)
- 1 главный скрипт воркспейсов (tail mode)
- 4 вспомогательных скрипта
- 2 rofi темы
- 2 цветовых схемы
- ~16 параметров layout
- 22+ Nerd Font иконки

**Технический долг:**
- 3 захардкожены константы (EDGE_PADDING, GAP, ICON_WIDTH)
- Brute-force полный рестарт вместо горячей перезагрузки
- Отсутствие визуальной верификации (Dracula тема, multi-monitor)

**Готовность к миграции:** 90% — можно переносить как есть, затем рефакторить debt в отдельной итерации.
