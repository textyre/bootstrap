---
all:
  children:
    workstations:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
        # Remote hosts — uncomment and configure when needed:
        # archvm:
        #   ansible_host: 192.168.56.10
        #   ansible_user: user
        # gentoobox:
        #   ansible_host: 192.168.56.11
        #   ansible_user: user
