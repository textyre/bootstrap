---
- name: Skip Fedora packages install because backend is not implemented
  ansible.builtin.debug:
    msg: >-
      packages role backend for Fedora/RedHat is not implemented yet; install
      phase is skipped.
  tags: ['packages', 'install']
