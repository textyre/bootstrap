---
- name: Restart caddy
  ansible.builtin.command:
    cmd: docker compose -f {{ caddy_base_dir }}/docker-compose.yml restart
  listen: Restart caddy

- name: Reload caddy
  ansible.builtin.command:
    cmd: docker exec caddy caddy reload --config /etc/caddy/Caddyfile
  listen: Reload caddy
