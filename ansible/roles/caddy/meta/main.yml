---
galaxy_info:
  role_name: caddy
  author: textyre
  description: Reverse proxy (Caddy) — единый HTTPS proxy для self-hosted сервисов
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: ArchLinux
      versions: [all]
  galaxy_tags: [caddy, proxy, https, reverseproxy]
dependencies:
  - role: docker
