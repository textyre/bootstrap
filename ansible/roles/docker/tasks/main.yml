---
# === Конфигурация Docker ===
# daemon.json, сервис, группа пользователя
# NOTE: docker group membership = эквивалент root доступа (документированное поведение Docker)

# ---- Конфигурация daemon ----

- name: Ensure /etc/docker directory exists
  ansible.builtin.file:
    path: /etc/docker
    state: directory
    owner: root
    group: root
    mode: '0755'
  tags: ['docker', 'configure']

- name: Deploy Docker daemon.json
  ansible.builtin.template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: '0644'
  notify: Restart docker
  tags: ['docker', 'configure']

# ---- Группа пользователя ----

- name: Ensure docker group exists
  ansible.builtin.group:
    name: docker
    state: present
  when: docker_add_user_to_group
  tags: ['docker', 'configure']

- name: Add user to docker group
  ansible.builtin.user:
    name: "{{ docker_user }}"
    groups: docker
    append: true
  when: docker_add_user_to_group
  tags: ['docker', 'configure']

# ---- Сервис ----

- name: Enable and start docker service
  ansible.builtin.service:
    name: docker
    enabled: true
    state: started
  when: docker_enable_service
  tags: ['docker', 'service']
