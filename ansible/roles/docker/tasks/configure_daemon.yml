---
# Build and deploy /etc/docker/daemon.json from the validated role inputs.

- name: Ensure /etc/docker directory exists
  ansible.builtin.file:
    path: /etc/docker
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: "CIS 2.8 | CIS 2.1 | CIS 2.14 — Deploy Docker daemon.json"
  vars:
    _docker_daemon_config: >-
      {{
        {
          "storage-driver": docker_storage_driver | string | trim,
          "log-driver": docker_log_driver,
          "log-opts": ({
            "max-size": docker_log_max_size,
            "max-file": docker_log_max_file
          } if docker_log_driver != 'journald' else {})
        }
        | combine({"userns-remap": docker_userns_remap} if docker_userns_remap | string | trim | length > 0 else {})
        | combine({"icc": false} if not (docker_icc | bool) else {})
        | combine({"live-restore": true} if docker_live_restore | bool else {})
        | combine({"no-new-privileges": true} if docker_no_new_privileges | bool else {})
        | combine(docker_daemon_overwrite, recursive=True)
      }}
  ansible.builtin.template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: '0644'
    validate: 'python3 -m json.tool %s'
  notify: Restart docker
  tags: ['docker', 'docker:configure', 'security', 'cis_2.8', 'cis_2.1', 'cis_2.14']

- name: "Report: Configure daemon"
  ansible.builtin.include_role:
    name: common
    tasks_from: report_phase.yml
  vars:
    common_rpt_fact: "_docker_phases"
    common_rpt_phase: "Configure daemon"
    common_rpt_detail: >-
      log={{ docker_log_driver }}
      icc={{ docker_icc | string }}
      storage={{ docker_storage_driver | string | trim }}
  tags: ['docker', 'report']
