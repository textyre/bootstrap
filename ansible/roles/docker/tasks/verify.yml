---
# In-role verification proves the applied implementation is usable after the
# role runs. Full behavioral smoke and negative input matrices live in Molecule.

- name: Verify Docker daemon implementation
  ansible.builtin.include_tasks: verify_daemon.yml

- name: Verify Docker runtime implementation
  ansible.builtin.include_tasks: verify_runtime.yml
  when:
    - docker_enable_service | bool
    - ansible_facts['virtualization_type'] | default('') != 'docker'

- name: Docker verify complete
  ansible.builtin.debug:
    msg: >-
      Docker configuration verified on
      {{ ansible_facts['distribution'] }} {{ ansible_facts['distribution_version'] }}.
      daemon.json: OK. Storage: {{ docker_storage_driver | string | trim }}.
      Service: {{ 'enabled' if docker_enable_service | bool else 'skipped' }}.
