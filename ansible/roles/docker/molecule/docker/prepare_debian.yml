---
- name: "Prepare: update apt cache"
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: "Prepare: install docker.io package"
  ansible.builtin.apt:
    name: docker.io
    state: present
