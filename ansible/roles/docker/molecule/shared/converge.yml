---
- name: Apply Docker role implementation
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: docker

- name: Validate Docker role input contract
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Validate storage driver input contract
      ansible.builtin.include_tasks: validate/storage_driver.yml
