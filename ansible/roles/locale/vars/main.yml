---
# Supported os_family values for locale generation
# Used in main.yml for with_first_found dispatch and unsupported-OS warning
locale_supported_os_families:
  - archlinux
  - debian
  - redhat
  - void
