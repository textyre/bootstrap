---
# Настройка системной локали (glibc)
# /etc/locale.conf с LANG и LC_* overrides

- name: Set default locale (locale.conf)
  ansible.builtin.template:
    src: locale.conf.j2
    dest: "{{ _locale_config_path }}"
    owner: root
    group: root
    mode: '0644'
  tags: ['locale']
