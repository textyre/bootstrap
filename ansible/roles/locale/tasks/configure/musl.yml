---
# Настройка системной локали (Alpine / musl)
# /etc/profile.d/10-locale.sh

- name: Set locale via profile.d (Alpine)
  ansible.builtin.template:
    src: locale-alpine.sh.j2
    dest: /etc/profile.d/10-locale.sh
    owner: root
    group: root
    mode: '0755'
  tags: ['locale']
