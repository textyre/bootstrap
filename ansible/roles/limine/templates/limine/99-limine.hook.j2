[Trigger]
Operation = Install
Operation = Upgrade
Type = Package
Target = limine

[Action]
Description = Deploying Limine after upgrade...
When = PostTransaction
# The detailed boot-update flow lives in a managed helper script so the hook
# stays small and the maintenance contract is readable.
Exec = {{ limine_pacman_hook_script_file }}
