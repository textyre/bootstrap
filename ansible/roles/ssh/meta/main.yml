---
galaxy_info:
  role_name: ssh
  author: textyre
  description: Hardened OpenSSH server configuration
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: ArchLinux
      versions: [all]
    - name: Debian
      versions: [all]
    - name: Ubuntu
      versions: [all]
  galaxy_tags: [ssh, security, hardening, sshd, crypto]
dependencies: []
# Collection dependencies declared in ansible/requirements.yml:
#   - community.crypto >=2.0.0  (community.crypto.openssh_keypair for host keys)
