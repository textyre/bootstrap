---
# Role dependency used by report_phase.yml and report_render.yml.
# Molecule resolves this from the monorepo through ANSIBLE_ROLES_PATH.
roles:
  - name: common

# Collection dependencies are declared at project level in ansible/requirements.yml:
#   - ansible.posix >=1.5.0
#   - community.crypto >=2.0.0
