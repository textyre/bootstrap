---
# === Внутренние маппинги (не для пользователя) ===

_hostname_strategy_by_os:
  Archlinux: systemd
  Debian: debian
  RedHat: redhat
  Void: generic
  Gentoo: generic
_hostname_strategy: "{{ _hostname_strategy_by_os[ansible_facts['os_family']] }}"
