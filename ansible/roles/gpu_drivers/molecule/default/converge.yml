---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  pre_tasks:
    - name: Ensure pciutils is installed (required for GPU auto-detection)
      ansible.builtin.package:
        name: pciutils
        state: present

  roles:
    - role: gpu_drivers
