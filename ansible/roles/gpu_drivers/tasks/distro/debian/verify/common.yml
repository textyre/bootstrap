---
# === gpu_drivers / verify / common ===

- name: "Assert Vulkan tools packages are installed"
  ansible.builtin.include_tasks:
    file: common/verify_packages.yml
  vars:
    _gpu_drivers_package_stack: "{{ _gpu_drivers_debian_vulkan_tools }}"
  when: gpu_drivers_vulkan_tools
