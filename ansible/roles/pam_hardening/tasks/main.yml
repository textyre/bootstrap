---
# === PAM hardening ===
# Конфигурация PAM-модулей безопасности

- name: Configure PAM faillock
  ansible.builtin.include_tasks: faillock.yml
  when: pam_hardening_faillock_enabled
  tags: ['pam', 'security', 'faillock']
