---
# === Verify console keymap: systemd backend router ===

- name: Verify console keymap (systemd, vconsole.conf)
  ansible.builtin.include_tasks: systemd/vconsole.yml
  when: ansible_facts['os_family'] != 'Debian'
  tags: ['vconsole']
