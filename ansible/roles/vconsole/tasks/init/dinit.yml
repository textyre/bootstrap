---
# === Console keymap — dinit init system (stub) ===

- name: Unsupported init system (dinit) — console keymap skipped
  ansible.builtin.debug:
    msg: "Console keymap configuration skipped: dinit init system is not yet supported by vconsole role."
  tags: ['vconsole']
