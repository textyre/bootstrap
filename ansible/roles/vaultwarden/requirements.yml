---
# Local prerequisite roles are resolved from the adjacent source tree through
# ANSIBLE_ROLES_PATH in Molecule and the project playbook environment.
roles:
  - name: common
  - name: caddy
  - name: docker
