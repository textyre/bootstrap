#!/usr/sbin/nft -f
# {{ ansible_managed }}
# Базовый firewall для рабочей станции
#
# Own only the firewall's inet filter table. Do not flush the whole ruleset:
# Docker programs separate iptables-nft / nft tables for bridge networking.
# Create the table if missing, then delete and recreate it atomically so the
# ruleset stays reloadable on both first apply and later updates.
table inet filter
delete table inet filter

table inet filter {
{% if firewall_allow_ssh and firewall_ssh_rate_limit_enabled %}
    # CRIT-01: per-source-IP SSH rate limiting — IPv4
    set ssh_ratelimit {
        type ipv4_addr
        flags dynamic,timeout
        timeout 1m
    }

    # CRIT-01: per-source-IP SSH rate limiting — IPv6
    set ssh6_ratelimit {
        type ipv6_addr
        flags dynamic,timeout
        timeout 1m
    }

{% endif %}
    chain input {
        type filter hook input priority 0; policy drop;

        # Разрешить loopback
        iifname "lo" accept

        # Отбросить пакеты с невалидным состоянием
        ct state invalid drop

        # Разрешить установленные соединения
        ct state established,related accept

        # Разрешить ICMP с rate limiting (защита от flood)
        ip protocol icmp limit rate 10/second accept
        ip6 nexthdr icmpv6 limit rate 10/second accept

{% if firewall_allow_ssh %}
{% if firewall_ssh_rate_limit_enabled %}
        # SSH с per-source-IP rate limiting IPv4 (CRIT-01)
        tcp dport 22 ct state new add @ssh_ratelimit { ip saddr limit rate over {{ firewall_ssh_rate_limit }} burst {{ firewall_ssh_rate_limit_burst }} packets } log prefix "[nftables] ssh-rate: " drop
        # SSH с per-source-IP rate limiting IPv6 (CRIT-01)
        tcp dport 22 ct state new add @ssh6_ratelimit { ip6 saddr limit rate over {{ firewall_ssh_rate_limit }} burst {{ firewall_ssh_rate_limit_burst }} packets } log prefix "[nftables] ssh6-rate: " drop
{% endif %}
        # SSH — разрешить
        tcp dport 22 ct state new accept
{% endif %}
{% for port in firewall_allow_tcp_ports %}
        tcp dport {{ port }} accept
{% endfor %}
{% for port in firewall_allow_udp_ports %}
        udp dport {{ port }} accept
{% endfor %}

        # Логировать и отбросить остальное
        log prefix "[nftables] drop: " counter drop
    }

    chain forward {
        type filter hook forward priority 0; policy drop;

        # Разрешить установленные/связанные (общее правило)
        ct state established,related accept

{% if firewall_docker_enabled %}
        # Docker bridges — разрешить новые соединения для default bridge и
        # user-defined bridge networks (for example the shared "proxy" network)
        iifname "docker0" ct state new accept
        oifname "docker0" ct state new accept
        iifname "br-*" ct state new accept
        oifname "br-*" ct state new accept
{% endif %}
    }

    chain output {
        type filter hook output priority 0; policy accept;
    }
}
