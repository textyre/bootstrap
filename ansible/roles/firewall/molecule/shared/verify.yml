---
- name: Verify firewall keeps SSH access usable
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Close the connection established before firewall activation
      ansible.builtin.meta: reset_connection

    - name: Confirm a new Ansible connection succeeds through the active firewall
      ansible.builtin.ping:
