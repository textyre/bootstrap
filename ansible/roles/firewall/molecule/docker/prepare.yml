---
- name: Prepare
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: Wait for systemd to reach multi-user target
      ansible.builtin.command: systemctl is-system-running --wait
      register: _prepare_systemd_state
      changed_when: false
      failed_when: false
      timeout: 30
