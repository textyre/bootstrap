---
# Handlers for ntp role

- name: Restart chronyd
  ansible.builtin.service:
    name: "{{ ntp_service[ansible_facts['service_mgr']] | default('chronyd') }}"
    state: restarted
  listen: "restart ntp"
