---
# === Gentoo: package manager configuration stub ===
# Gentoo support is declared for completeness (ROLE-003).
# Implement portage configuration tasks when a Gentoo host is enrolled.

- name: Gentoo package manager — stub
  ansible.builtin.debug:
    msg: "Gentoo package_manager support is a stub — no configuration applied"
  tags: ['packages', 'portage']
