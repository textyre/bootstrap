---
# === Login shell configuration ===
# Install a shell and assign it to an existing user.
# PATH, environment variables, and per-user dotfiles are managed outside this role.

# Existing target user whose login shell is managed.
shell_user: "{{ target_user }}"

# Shell to install and configure. Allowed: bash, zsh, fish.
shell_type: zsh
