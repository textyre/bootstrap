---
# === Shell role — internal variables ===

# Supported shell types
_shell_supported_types:
  - bash
  - zsh
  - fish

# OS families with package and path mappings implemented by this role.
_shell_supported_os:
  - Archlinux
  - Debian
  - RedHat
  - Void
  - Gentoo
