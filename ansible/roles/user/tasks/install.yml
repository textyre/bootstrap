---
# === Install sudo (all platforms) ===
# Package name is defined per OS family in vars/<os_family>.yml
- name: Ensure sudo is installed
  ansible.builtin.package:
    name: "{{ user_packages }}"
    state: present
  tags: [user, sudo, install]
