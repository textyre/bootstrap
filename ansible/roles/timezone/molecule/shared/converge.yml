---
- name: Converge
  hosts: all
  become: true
  gather_facts: true

  roles:
    - role: timezone
      vars:
        timezone_name: "Asia/Almaty"
        timezone_packages_tzdata:
          Archlinux: "tzdata"
          Debian: "tzdata"
          default: "tzdata"
