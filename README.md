# 🐧 Arch Linux Utilities Collection

Коллекция утилит для работы с Arch Linux: анализ зависимостей пакетов и автоматический bootstrap изолированных контейнерных окружений.

## 📦 Проекты

### 1. Package Dependency Tree Scripts
Набор скриптов для анализа установленных пакетов и их зависимостей в Arch Linux с автоматической синхронизацией через SSH.

**Подробнее:** Смотрите разделы ниже.

### 2. Containerized Environment Bootstrap ⭐ NEW
Автоматический bootstrap минимальных контейнерных окружений (Podman/Docker + distrobox) для изолированного запуска приложений и desktop WM (i3, Sway) на чистых VM.

**📖 Документация:** [scripts/bootstrap/README.md](scripts/bootstrap/README.md)

**Быстрый старт:**
```bash
chmod +x scripts/bootstrap/**/*.sh scripts/status.sh
./scripts/bootstrap/bootstrap.sh  # Interactive setup
./scripts/status.sh               # Check system status
```

**Особенности:**
- ✅ Минимум зависимостей на хосте (3-5 пакетов)
- ✅ Rootful режим по умолчанию (избегаем лишних зависимостей)
- ✅ Полный трекинг транзитивных зависимостей
- ✅ Откат на всех уровнях (контейнеры/образы/пакеты)
- ✅ Профильная изоляция (каждая VM = новый профиль)
- ✅ Desktop WM support (GPU/input device passthrough)

---

## 📁 Структура проекта

```
arch/
├── scripts/
│   ├── bootstrap/              # 🆕 Containerized environment bootstrap
│   │   ├── bootstrap.sh             # Interactive setup script
│   │   ├── install/                 # Installation modules
│   │   │   ├── engine-select.sh          # Podman/Docker installer
│   │   │   └── create-env.sh             # Environment builder
│   │   ├── rollback/                # Rollback utilities
│   │   │   └── rollback.sh               # Multi-level cleanup
│   │   ├── session-manager.sh       # Environment session manager
│   │   └── containers/              # Container definitions
│   │       └── base.Containerfile        # Minimal base image
│   ├── status.sh                    # 🆕 System diagnostics + original status
│   ├── show_installed_packages.sh   # Пакеты и прямые зависимости
│   └── show_all_dependencies.sh     # Полное дерево зависимостей
│
├── profiles/                   # 🆕 VM profiles (generated)
│   └── <profile-id>/
│       ├── state/
│       ├── dotfiles/
│       ├── overlay/
│       └── logs/
│
├── windows/                    # Windows утилиты для синхронизации
│   ├── config.ps1                    # ⚙️  Общая конфигурация (НАСТРОЙТЕ ЗДЕСЬ)
│   ├── setup_ssh_key.ps1             # 🔑 Настройка SSH ключа (3 шага)
│   ├── sync_to_server.ps1            # 🔄 Синхронизация файлов
│   ├── show-config.ps1               # 📋 Просмотр конфигурации
│   ├── test-connection.ps1           # 🔌 Проверка подключения
│   │
│   ├── modules/                      # Внутренние модули
│   │   ├── ssh-keygen.ps1                 # Генерация ключа
│   │   ├── ssh-copy-id.ps1                # Копирование ключа
│   │   └── ssh-config.ps1                 # Обновление SSH config
│   │
│   ├── *.bat                         # Обертки для двойного клика
│   └── README.md                     # Документация Windows утилит
│
├── docs/                       # Документация
│   ├── QUICKSTART.md                 # Быстрый старт
│   └── CONFIG.txt                    # Справка по настройке
│
├── .gitignore
├── USAGE.md                    # Краткая шпаргалка
└── README.md                   # Этот файл
```

## 🚀 Быстрый старт

### 1. Настройте параметры подключения

Откройте **`windows/config.ps1`** и измените настройки:

```powershell
$Global:SERVER_USER = "your_username"     # Ваш пользователь
$Global:SERVER_HOST = "192.168.1.100"     # IP адрес сервера
$Global:SERVER_PORT = 22                  # Порт SSH
$Global:REMOTE_PATH = "/home/your_username/scripts"  # Путь на сервере
```

**ВАЖНО:** Все остальные скрипты используют этот файл!

### 2. Настройте SSH ключ (один раз)

Для автоматической синхронизации без ввода пароля:

```powershell
cd windows
.\setup_ssh_key.ps1
```

Скрипт выполнит 3 шага:
1. Сгенерирует уникальный SSH ключ
2. Скопирует его на сервер (потребуется пароль **ОДИН РАЗ**)
3. Обновит SSH конфигурацию

После этого все подключения будут автоматическими!

### 3. Синхронизируйте скрипты на сервер

```powershell
cd windows
.\sync_to_server.ps1
```

### 4. Запустите на Arch Linux

```bash
ssh -p 2222 user@server
cd scripts
./show_installed_packages.sh
./show_all_dependencies.sh
```

## 📝 Описание скриптов

### Скрипты для Arch Linux (`scripts/`)

#### `show_installed_packages.sh`
Показывает явно установленные пакеты и их **прямые зависимости** (первый уровень).

**Пример вывода:**
```
┌─ firefox (120.0-1)
│  ├─ gtk3 (3.24.38-1)
│  ├─ libxrandr (1.5.3-1)
│  └─ nss (3.96-1)
```

#### `show_all_dependencies.sh`
Показывает полное дерево **всех зависимостей рекурсивно** (все уровни вложенности).

**Пример вывода:**
```
firefox (120.0-1)
├─ gtk3 (3.24.38-1)
│  ├─ cairo (1.18.0-1)
│  │  └─ fontconfig (2.15.0-1)
│  └─ glib2 (2.78.3-1)
└─ nss (3.96-1)
   └─ nspr (4.35-1)
```

### Windows утилиты (`windows/`)

#### `config.ps1` ⚙️
**Общий конфигурационный файл** для всех скриптов.
- ✅ Единое место для настроек подключения
- ✅ Используется всеми остальными скриптами
- ✅ Автоматически вычисляет пути к ключам

#### `setup_ssh_key.ps1` 🔑
**Модульная настройка SSH ключа** (3 шага):
1. Генерация уникального ключа (Ed25519)
2. Копирование на сервер
3. Обновление SSH config

#### `sync_to_server.ps1` 🔄
**Синхронизация скриптов** на Arch сервер:
- ✅ Передает только измененные файлы (через rsync)
- ✅ Автоматически устанавливает права на выполнение
- ✅ Работает без пароля после настройки
- ✅ Использует конфигурацию из `config.ps1`

#### `show-config.ps1` 📋
Показывает текущую конфигурацию и статус подключения.

#### `test-connection.ps1` 🔌
Проверяет подключение к серверу для диагностики.

### Модули (`windows/modules/`)

Внутренние модули для повторного использования кода:
- `ssh-keygen.ps1` - генерация SSH ключей
- `ssh-copy-id.ps1` - копирование ключей на сервер
- `ssh-config.ps1` - управление SSH конфигурацией

## 🔒 Безопасность

Проект использует **отдельный SSH ключ** для каждого сервера:
- Уникальное имя: `id_rsa_arch_<host>_<port>`
- Если ключ скомпрометирован - доступ только к одному серверу
- Современный алгоритм Ed25519
- Автоматическое управление через SSH config

## ⚙️ Требования

### На Arch Linux:
- `pacman` (встроенный)
- Стандартные утилиты: `grep`, `sed`, `awk`, `tr`
- SSH сервер (openssh)

### На Windows:
- PowerShell 5.1+ (встроенный в Windows 10/11)
- SSH клиент (встроенный в Windows 10/11)
- **Опционально:** `rsync` для инкрементальной синхронизации

## 📚 Документация

- **[QUICKSTART.md](docs/QUICKSTART.md)** - Пошаговая инструкция для начинающих
- **[CONFIG.txt](docs/CONFIG.txt)** - Справка по настройке параметров

## 💡 Примеры использования

### Быстрая синхронизация и запуск

```powershell
# На Windows
cd windows
.\sync_to_server.ps1

# Затем на сервере
ssh -p 2222 user@server "cd scripts && ./show_installed_packages.sh"
```

### Сохранение результатов в файл

```bash
# На Arch Linux
./show_installed_packages.sh > packages_tree.txt
./show_all_dependencies.sh > full_dependencies.txt
```

### Анализ конкретного пакета

```bash
# Вручную с pacman
pacman -Qi firefox | grep "Depends On"
```

## 🛠️ Дополнительные возможности

### Установка rsync на Windows

Для более быстрой синхронизации (передаются только изменения):

```powershell
# Через winget
winget install rsync

# Или уже включен в Git for Windows
```

### Автоматизация с Task Scheduler

Можно настроить автоматическую синхронизацию по расписанию в Windows Task Scheduler.

## 🆘 Решение проблем

### "Permission denied" при подключении

```powershell
cd windows
.\setup_ssh_key.ps1
```

### "Connection refused"

Проверьте, что SSH сервер запущен:

```bash
# На Arch Linux
sudo systemctl status sshd
sudo systemctl start sshd
sudo systemctl enable sshd
```

### Скрипт запрашивает пароль каждый раз

Запустите настройку SSH ключа:

```powershell
cd windows
.\setup_ssh_key.ps1
```

## 📄 Лицензия

Свободное использование. Создано для анализа пакетов в Arch Linux.

## 🤝 Вклад

Проект создан для личного использования. Вы можете свободно модифицировать под свои нужды.

---

**Автор:** Скрипты созданы для удобного анализа зависимостей пакетов в Arch Linux  
**Версия:** 1.0.0
