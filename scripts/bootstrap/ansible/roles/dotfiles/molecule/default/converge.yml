---
# Molecule converge playbook
# Применяет роль dotfiles для тестирования

- name: Converge
  hosts: all
  become: true
  gather_facts: true
  vars_files:
    - ../../../../inventory/group_vars/all/vault.yml

  roles:
    - role: dotfiles
