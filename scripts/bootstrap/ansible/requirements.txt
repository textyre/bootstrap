# Core Ansible
ansible==13.2.0
ansible-core==2.20.1

# Linting and validation
ansible-lint==26.1.1
yamllint==1.38.0

# Testing framework
molecule==25.12.0
# Delegated driver - позволяет тестировать роль на localhost без Docker
# Нужен для Arch-специфичных ролей, требующих systemd и pacman
molecule-plugins[delegated]>=23.5.0

# Additional useful tools
jmespath==1.1.0
