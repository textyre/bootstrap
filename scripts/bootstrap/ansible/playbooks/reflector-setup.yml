---
- name: Setup reflector and refresh pacman mirrorlist (role)
  hosts: localhost
  become: true
  gather_facts: true
  vars:
    reflector_countries: "US"
    reflector_args: "--protocol https --latest 10 --sort rate"
  roles:
    - role: reflector
      tags: ['reflector']
