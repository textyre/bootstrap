{{- $t := index .themes .theme_name -}}
/* Rofi App Launcher — Cyberpunk Theme */

* {
    bg:       {{ $t.bg }}ee;
    bg-alt:   {{ $t.bg_light }}cc;
    fg:       {{ $t.fg }};
    fg-dim:   {{ $t.fg_dim }};
    accent:   {{ $t.accent }};
    accent2:  {{ $t.accent2 }};
    urgent:   {{ $t.urgent }};
    border:   {{ $t.border_active }};

    background-color: transparent;
    text-color: @fg;
    font: "{{ .font.mono }} {{ .font.mono_size }}";
}

window {
    width: {{ .rofi.launcher_width }}px;
    transparency: "real";
    background-color: @bg;
    border: 1px;
    border-color: @border;
    border-radius: {{ .layout.corner_radius }}px;
    padding: {{ .rofi.padding_large }}px;
}

mainbox {
    spacing: {{ .rofi.spacing_large }}px;
    children: [ inputbar, listview ];
}

inputbar {
    children: [ prompt, entry ];
    background-color: @bg-alt;
    border-radius: {{ .rofi.border_radius_large }}px;
    padding: {{ .rofi.spacing_medium }}px;
}

prompt {
    text-color: @accent;
    padding: {{ .rofi.prompt_padding }};
}

entry {
    placeholder: "Search...";
    placeholder-color: @fg-dim;
}

listview {
    lines: {{ .rofi.launcher_lines }};
    columns: 1;
    fixed-height: true;
    spacing: {{ .rofi.spacing_element }}px;
    scrollbar: false;
}

element {
    padding: {{ .rofi.element_padding_large }};
    border-radius: {{ .rofi.border_radius_small }}px;
    spacing: {{ .rofi.element_icon_gap }}px;
}

element selected {
    background-color: @bg-alt;
    text-color: @accent;
}

element-icon {
    size: {{ .rofi.icon_size_launcher }}px;
}

element-text {
    vertical-align: 0.5;
}
