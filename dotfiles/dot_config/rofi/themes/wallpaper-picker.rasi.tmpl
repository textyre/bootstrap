{{- $t := index .themes .theme_name -}}
/* Rofi Wallpaper Picker — Cyberpunk Theme */

* {
    bg:      {{ $t.bg }}ee;
    bg-alt:  {{ $t.bg_light }}cc;
    fg:      {{ $t.fg }};
    fg-dim:  {{ $t.fg_dim }};
    accent:  {{ $t.accent }};
    border:  {{ $t.border_active }};

    background-color: transparent;
    text-color: @fg;
    font: "{{ .font.mono }} {{ .font.mono_size }}";
}

window {
    width: {{ .rofi.wallpaper_width }}px;
    transparency: "real";
    background-color: @bg;
    border: 1px;
    border-color: @border;
    border-radius: {{ .layout.corner_radius }}px;
    padding: {{ .rofi.padding_large }}px;
}

mainbox {
    spacing: {{ .rofi.spacing_medium }}px;
    children: [ inputbar, listview ];
}

inputbar {
    children: [ prompt, entry ];
    background-color: @bg-alt;
    border-radius: {{ .rofi.border_radius_large }}px;
    padding: {{ .rofi.spacing_medium }}px;
}

prompt {
    text-color: @accent;
    padding: {{ .rofi.prompt_padding }};
}

entry {
    placeholder: "Filter...";
    placeholder-color: @fg-dim;
}

listview {
    lines: {{ .rofi.wallpaper_lines }};
    columns: 1;
    fixed-height: true;
    spacing: {{ .rofi.spacing_element }}px;
    scrollbar: false;
}

element {
    padding: {{ .rofi.element_padding_large }};
    border-radius: {{ .rofi.border_radius_small }}px;
}

element selected {
    background-color: @bg-alt;
    text-color: @accent;
}

element-text {
    vertical-align: 0.5;
}
