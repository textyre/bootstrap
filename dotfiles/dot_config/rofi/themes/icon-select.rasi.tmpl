{{- $t := index .themes .theme_name -}}
/* Rofi Icon Select — Grid of icons */

* {
    bg:      {{ $t.bg }}ee;
    fg:      {{ $t.fg }};
    fg-dim:  {{ $t.fg_dim }};
    accent:  {{ $t.accent }};
    urgent:  {{ $t.urgent }};
    border:  {{ $t.border_active }};
    bg-light: {{ $t.bg_light }};

    background-color: transparent;
    text-color: @fg;
    font: "{{ .font.mono }} {{ .font.mono_size }}";
}

window {
    width: {{ .rofi.icon_select_width }}px;
    location: northwest;
    x-offset: {{ .rofi.icon_select_x_offset }};
    y-offset: {{ .rofi.icon_select_y_offset }};
    transparency: "real";
    background-color: @bg;
    border: 1px;
    border-color: @border;
    border-radius: {{ .layout.corner_radius }}px;
    padding: {{ .rofi.spacing_medium }}px;
}

mainbox {
    children: [ inputbar, listview ];
    spacing: {{ .rofi.spacing_medium }}px;
}

inputbar {
    children: [ prompt ];
    padding: {{ .rofi.inputbar_padding_icon_select }};
    background-color: @bg-light;
    border-radius: {{ .rofi.border_radius_small }}px;
}

prompt {
    text-color: @accent;
}

listview {
    lines: {{ .rofi.icon_select_lines }};
    columns: {{ .rofi.icon_select_columns }};
    spacing: {{ .rofi.spacing_icon_select }}px;
    cycle: true;
    fixed-height: true;
    scrollbar: false;
}

element {
    padding: {{ .rofi.element_padding_icon_select }};
    border-radius: {{ .rofi.border_radius_small }}px;
    horizontal-align: 0.0;
}

element selected {
    background-color: {{ $t.bg_light }}cc;
    text-color: @accent;
}
